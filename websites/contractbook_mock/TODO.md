# Contractbook Mock -- TODO

> Status: ROUND 2 COMPLETE (AUDIT fixes applied)
> Last updated by: dev agent, 2026-04-11
> Research: `assets/README.md` | Data model: `assets/data_model.md` | Design: `DESIGN.md`

## Status Legend
- [ ] Not started
- [~] In progress
- [x] Done

---

## P0 -- Core Shell

Without these, the app cannot render. Dev implements these first.

- [x] **Project scaffold**: `npm create vite@latest contractbook_mock -- --template react` inside the existing `contractbook_mock/` directory, install deps: `react-router-dom`, `lucide-react`. Do NOT use Tailwind -- use plain CSS with design tokens from `DESIGN.md`.
- [x] **App layout**: Three-zone layout matching `DESIGN.md` section 4. Left sidebar (240px, `#FAFBFC` background, `1px solid #E5E7EB` right border). Top header bar (56px, white background, `1px solid #E5E7EB` bottom border). Main content area fills remaining space with `#F7F8FA` background and 24px padding. The sidebar and header are fixed; main content scrolls independently.
- [x] **Sidebar component** (`src/components/Sidebar.jsx`): At top, Contractbook wordmark/logo in `#1A1A2E` (bold "Contractbook" text with a small document icon). Below that, a "New Contract" primary button (full width, `#1C00FF` background, white text, 6px border-radius, `+` icon). Navigation section with items: "Contracts" (FileText icon), "Templates" (LayoutTemplate icon), "Tasks" (CheckSquare icon), "Contacts" (Users icon). Each item: 36px height, 8px 12px padding, 6px radius, 14px Inter, icon 18px, 10px gap. Active item: `#EEF2FF` background, `#1C00FF` text. Hover: `#F3F4F6` background. Below nav: collapsible "Folders" section header (12px uppercase `#6B7280`, chevron toggle) with nested folder tree -- each folder row shows folder icon + name, indent 16px per level, click to filter contracts. Bottom of sidebar: divider line, "Settings" item (Settings icon), user section showing avatar circle (32px, initials "SC" on `#1C00FF` background) + "Sarah Chen" name + "Acme Corporation" in 12px muted text.
- [x] **Header component** (`src/components/Header.jsx`): Left side shows breadcrumb text (current page name, e.g., "Contracts" in 18px semibold `#1A1A2E`). Right side shows: search button (Search icon, ghost button, opens search modal), notification bell (Bell icon with red dot badge showing unread count, opens dropdown), user avatar (32px circle, clicking opens a non-functional dropdown with "Profile", "Settings", "Log out" items).
- [x] **Routing** (`src/App.jsx`): BrowserRouter wrapping the shell layout. Routes: `/` redirects to `/contracts`, `/contracts` (contract list), `/contracts/:id` (contract detail/editor), `/templates` (template library), `/templates/:id` (template preview), `/tasks` (task dashboard), `/contacts` (contacts list), `/settings` (settings page), `/go` (state inspector). All routes render inside the main content area within the shell layout (sidebar + header always visible except on `/go`).
- [x] **State management** (`src/context/AppContext.jsx` + `src/utils/dataManager.js`): React Context providing global state and dispatch functions. `dataManager.js` exports `createInitialData()` per `assets/data_model.md`, `loadState()` (from localStorage keyed `"contractbook_state"`), `saveState(state)`. The context provides: `state`, `dispatch` (for mutations), and convenience methods: `addContract()`, `updateContract()`, `deleteContract()`, `addContact()`, `updateContact()`, `deleteContact()`, `addTemplate()`, `addTask()`, `updateTask()`, `markNotificationRead()`, `markAllNotificationsRead()`. Every state mutation calls `saveState()` to persist.
- [x] **`/go` endpoint** (`src/pages/Go.jsx`): Route `/go` renders a full-page JSON view (no sidebar/header) showing `{ initial_state, current_state, state_diff }`. `initial_state` is captured once on first load (store a deep copy). `current_state` is live state. `state_diff` is computed by deep-comparing the two and listing all changed paths with old/new values. Page background white, content in a `<pre>` tag with monospace font. Also support `?sid=<session_id>` query parameter for session isolation.
- [x] **Session isolation** (`vite.config.js`): Add a Vite dev server plugin that intercepts: `POST /post?sid=<sid>` (accepts `{"action":"set","state":{...}}` to inject state, `{"action":"set_current","state":{...}}` to update current only, `{"action":"reset"}` to reset to initial), `GET /go?sid=<sid>` (returns `{initial_state, current_state, state_diff}` as JSON), `GET /state?sid=<sid>` (alias for `/go`). Session data stored in a Map keyed by `sid`. The frontend reads `sid` from URL query params and uses it to namespace localStorage keys.
- [x] **Seed data**: Implement `createInitialData()` in `dataManager.js` with all entities from `assets/data_model.md`: 1 currentUser (Sarah Chen), 5 team members, 8 contacts, 8 folders (nested), 6 tags, 12 contracts (3 draft, 3 pending, 4 signed, 1 rejected, 1 expired) with realistic HTML content bodies (at least 3-4 paragraphs of legal-sounding text per contract), 8 templates with content, 6 tasks, ~50 activity entries distributed across contracts, 8 notifications (3 unread), ~10 comments, 5 saved views, and settings object.

---

## P1 -- Primary Features

Core features a user interacts with in the first 5 minutes. Implement after P0 shell is working.

- [x] **Contract list page** (`src/pages/Contracts.jsx`): Full-width table view. Top section: page title "Contracts" (24px bold), "Create new document" button (primary, right-aligned). Tab bar below with tabs: "All Documents" (shows total count), "Drafts" (count), "Pending" (count), "Signed" (count), "Rejected" (count), "Expired" (count). Active tab has blue bottom border and blue text per `DESIGN.md` tab styles. Below tabs: filter row with search input (left, 280px, Search icon inside, placeholder "Search contracts..."), sort dropdown ("Sort by: Created date (newest first)" / "Title A-Z" / "Title Z-A" / "Updated date" / "Status"), and a "Filter" button that opens a filter popover (filter by folder, tags, created by, date range). Table columns: checkbox (24px), Contract Title (flex-grow, shows title text + tag pills inline), Counterparty (company name from external party), Date (formatted "MMM DD, YYYY"), Signees (overlapping avatar circles, max 3 shown + "+N" overflow), Status (pill badge per `DESIGN.md` badge styles). Table rows are 52px height, hover shows `#F9FAFB` background. Clicking a row navigates to `/contracts/:id`. Clicking a tab filters the table to that status. Empty states show centered icon + "No contracts found" message + CTA button.
- [x] **Contract detail page** (`src/pages/ContractDetail.jsx`): Two-column layout. Top bar: back arrow button (navigates to `/contracts`), contract title (editable on click -- shows input field, press Enter or blur to save), status badge, action buttons right-aligned: "Edit" (secondary button, only for drafts, switches to editor mode), "Send for Signature" (primary button, only for drafts, opens send modal), "More" dropdown (duplicate, move to folder, download, delete). Left column (flex-grow): rendered HTML contract content in a styled container (max-width 800px, centered, white background card, 32px padding, article typography from `DESIGN.md` editor styles). Right column (320px, sticky): collapsible panels -- (1) "Parties & Signees" panel: lists each party (name, type badge "Internal"/"External"), under each party lists signees with name, email, role, and signing status icon (green check for signed, amber clock for pending, red X for rejected, gray dash for not sent); (2) "Details" panel: key-value rows for Created date, Updated date, Expires date, Value/Currency, Folder location, Created by; (3) "Tags" panel: shows tag pills with X to remove, "+ Add tag" button opens a dropdown of available tags; (4) "Activity" panel: reverse-chronological list of activity entries, each showing icon (based on type), description text, relative timestamp ("2 hours ago"), scrollable max-height 400px.
- [x] **Contract editor mode** (`src/components/ContractEditor.jsx`): When editing a draft contract, the left column switches to an editable rich text area. Use `contentEditable` div or a simple custom editor (NOT a heavy library). Toolbar fixed above the editor with buttons: Bold (B), Italic (I), Underline (U), Strikethrough (S), separator, Heading 1, Heading 2, Heading 3, separator, Bullet List, Numbered List, Checkbox List, separator, Block Quote, Horizontal Rule, separator, Undo, Redo. Toolbar buttons are 32px square, ghost style, active state shows `#EEF2FF` background + blue icon. The editor area has white background, 24px padding, min-height 600px, body text 15px/24px. Typing updates the contract's `content` field in state. "Save" button (primary) in the top bar saves and exits edit mode. "Cancel" button (secondary) discards changes.
- [x] **Create new contract flow**: Clicking "New Contract" in sidebar or "Create new document" on contracts page opens a modal (640px wide) with two options displayed as large clickable cards: (1) "Blank Document" card -- icon FileText, subtitle "Start from scratch", click creates a new contract with empty content, status "draft", and navigates to `/contracts/:newId` in edit mode; (2) "From Template" card -- icon LayoutTemplate, subtitle "Use a template", click navigates to `/templates` page with a `?select=true` query param that shows "Use this template" buttons on each template card. When a template is selected, a new contract is created with the template's content copied, and navigates to the new contract in edit mode.
- [x] **Send for signature modal** (`src/components/SendForSignatureModal.jsx`): Multi-step modal (640px). Step 1 "Parties": shows existing parties or prompts to add. "Add Party" button creates a new party row with fields: Party Name (input), Type (internal/external toggle). Under each party, "Add Signee" button adds a signee row: Name, Email, Role inputs. For internal signees, show a dropdown of team members. For external, show a dropdown of contacts or allow manual entry. Step 2 "Signing Order": toggle "Enable signing order" -- when on, shows numbered drag handle next to each signee to set order (simulate with up/down arrow buttons). Step 3 "Review & Send": summary of all parties and signees, text area for "Message to recipients" (placeholder "Add a message that will be included in the signing invitation email..."), "Send" primary button. On send: contract status changes from "draft" to "pending", `sentAt` is set to now, all signee statuses set to "pending", activity entry "Contract sent for signature" is added, notification is created.
- [x] **Template library page** (`src/pages/Templates.jsx`): Page title "Templates" (24px bold), "Create Template" button (primary, right-aligned). Below: category filter bar (horizontal pills: "All", "Service Agreements", "NDAs", "Employment", "Procurement", "Licensing", "Partnerships", "Real Estate" -- active pill is `#1C00FF` background white text, others are outline). Grid layout of template cards (3 columns, 16px gap). Each card: white background, `1px solid #E5E7EB` border, 8px radius, 16px padding. Card contents: template title (16px semibold), description (14px muted, 2-line clamp), bottom row: category badge (small pill), usage count ("Used 12 times" in 12px muted), "Use Template" button (small primary button). Hovering a card shows subtle shadow. Clicking a card title opens template preview. If `?select=true` is in URL, "Use Template" button creates a new contract from this template.
- [x] **Template preview modal** (`src/components/TemplatePreview.jsx`): Modal (900px wide, 80vh height) showing the template content rendered as read-only HTML in a scrollable area (same styling as contract detail content). Header shows template title, category, and description. Footer has "Use Template" button (primary) and "Close" button (secondary).
- [x] **Folder navigation**: In the sidebar, the Folders section shows a tree of folders from state. Each folder row: FolderOpen icon (16px), folder name (14px). Nested folders indent 16px per level. Clicking a folder filters the contracts list to show only contracts in that folder (and sub-folders). The currently selected folder is highlighted with `#EEF2FF` background and `#1C00FF` text. A "All Contracts" option at the top clears the folder filter. Right-clicking a folder (or clicking a `...` icon on hover) shows a context menu: Rename, Create Subfolder, Delete. Creating a subfolder shows an inline input field. Renaming shows an inline edit input.
- [x] **Contacts page** (`src/pages/Contacts.jsx`): Page title "Contacts" (24px bold), "Add Contact" button (primary, right-aligned). Search input below title (full width, placeholder "Search contacts..."). Table with columns: Name (first + last, bold), Email, Company, Job Title, Phone, Created Date. Rows are clickable -- clicking opens an edit modal. Table supports sorting by clicking column headers (toggle asc/desc, show arrow icon).
- [x] **Contact modal** (`src/components/ContactModal.jsx`): Modal (480px) for creating or editing a contact. Form fields: First Name (required), Last Name (required), Email (required, email validation), Company, Job Title, Phone, Notes (textarea). Footer: "Save" primary button, "Cancel" secondary button, "Delete" danger button (only in edit mode, shows confirmation dialog). On save, updates or creates contact in state.
- [x] **Task dashboard** (`src/pages/Tasks.jsx`): Page title "Tasks" (24px bold), "Create Task" button (primary, right-aligned). Tab bar: "Assigned to me" (default, filters by current user as assignee), "Following" (tasks on contracts the user created), "All" (no filter). Task list: each task is a card-style row (white background, border, 8px radius, 12px padding). Shows: task title (14px semibold), type badge ("Approval" amber, "Renewal" blue, "Review" purple), linked contract name (clickable link to `/contracts/:id`), assignee avatar + name, due date (with red text if overdue), checkbox to mark complete. Clicking the checkbox toggles `status` between "pending" and "completed" (adds strikethrough to title). "Create Task" opens a modal with fields: Title, Type (dropdown: Approval/Renewal/Review), Contract (dropdown of contracts), Assignee (dropdown of team members), Due Date (date input).
- [x] **Notifications dropdown** (`src/components/NotificationDropdown.jsx`): Clicking the bell icon in the header opens a dropdown panel (360px wide, max-height 480px, anchored to bell icon, shadow-lg). Header: "Notifications" title, "Mark all as read" text button (right-aligned). List of notification items: each shows icon (based on type -- PenTool for signature, Eye for viewed, CheckSquare for task, AlertCircle for expired, Bell for reminder), title (14px, bold if unread), description (12px muted), relative timestamp (12px muted), unread dot (blue circle on left if unread). Clicking a notification marks it as read and navigates to the relevant contract. Unread count in the header badge equals the number of notifications with `read: false`.
- [x] **Search modal** (`src/components/SearchModal.jsx`): Clicking the search button in header opens a centered modal with a large search input at top (auto-focused, 18px font, Search icon, placeholder "Search contracts, templates, contacts..."). Below the input, show live filtered results grouped by type: "Contracts" section (matching by title), "Templates" section (matching by title), "Contacts" section (matching by name or email). Each result row: icon, title, subtitle (status for contracts, category for templates, company for contacts). Clicking a result navigates to the relevant page. Pressing Escape closes the modal. Support Cmd/Ctrl+K keyboard shortcut to open.
- [x] **Status transitions**: Implement the following status change logic as actions: (1) Draft -> Pending: triggered by "Send for Signature" flow; (2) Pending -> Signed: triggered when all signees have signed (simulate by clicking "Mark as Signed" on a signee in the detail view); (3) Pending -> Rejected: triggered when any signee rejects (simulate with a "Reject" action); (4) Any -> Expired: computed based on `expiresAt` date. When a signee status changes, add an activity entry and create a notification. When all signees sign, contract status auto-updates to "signed" with `signedAt` set to now.

---

## P2 -- Secondary Features

Depth and realism. Implement only after P1 is solid.

- [x] **Contract comments panel**: In the contract detail right sidebar, add a "Comments" collapsible panel below Activity. Shows a list of comments: each displays user avatar (24px), user name (12px semibold), timestamp (12px muted), comment text (14px). At the bottom, a textarea input ("Add a comment...") with "Post" button. Posting a comment adds to state, creates an activity entry, and shows immediately in the list. Comments can be marked as "resolved" (strikethrough + "Resolved" label).
- [ ] **Bulk actions on contracts list**: Clicking checkboxes in the contract table selects rows. When 1+ rows selected, a floating action bar appears at the bottom of the table (centered, shadow-lg, white background, rounded): shows count "N selected", buttons: "Move to Folder" (opens folder picker dropdown), "Add Tag" (opens tag picker dropdown), "Delete" (danger, shows confirmation). "Select All" checkbox in table header toggles all visible rows.
- [ ] **Custom saved views**: In the contracts list, add a "Views" dropdown next to the tabs. Shows saved views from state. Each view has a name and clicking it applies its filters/sort/columns. "Save Current View" option at bottom opens a modal to name and save the current filter+sort configuration. "Manage Views" option opens a modal to rename or delete saved views.
- [ ] **Drag-and-drop signee reordering**: In the Send for Signature modal (Step 2), when signing order is enabled, signees can be reordered by dragging (implement with simple mousedown/mousemove/mouseup handlers -- no library needed). Show a grip handle icon on the left of each signee row. Dragging a signee re-numbers the order sequence.
- [ ] **Contract value display**: In the contracts list table, add an optional "Value" column (hidden by default, can be toggled via a "Columns" dropdown button). Shows formatted currency value (e.g., "$150,000 USD"). In contract detail, show in the Details panel.
- [x] **Settings page** (`src/pages/Settings.jsx`): Tabbed layout with 3 tabs. "General" tab: Company Name input, Default Currency dropdown, Default Language dropdown, Timezone dropdown. "Notifications" tab: toggle switches for Email Notifications, In-App Notifications, Signing Reminders, Expiration Alerts, with reminder days multi-select (1, 3, 7 days before). "Team" tab: list of team members (name, email, role badge, status), "Invite Member" button (opens modal with email + role inputs). All settings save to state.
- [x] **Empty states**: For every list/table that can be empty, show a centered empty state: large muted icon (48px), heading "No [items] yet", description text, and a CTA button. Examples: "No contracts yet - Create your first contract", "No templates yet - Create a template", "No tasks assigned", "No contacts added".
- [x] **Contract duplication**: "Duplicate" action in the contract detail "More" dropdown creates a copy of the contract with title "Copy of [original title]", status reset to "draft", new ID, all signee statuses reset to "not_sent", and navigates to the new contract.
- [x] **Delete contract**: "Delete" action in "More" dropdown shows a confirmation dialog ("Are you sure you want to delete [title]? This action cannot be undone.") with "Delete" danger button and "Cancel" button. Deleting removes the contract from state and navigates back to `/contracts`.
- [x] **Move to folder**: "Move to Folder" in "More" dropdown shows a modal with the folder tree. Clicking a folder moves the contract (updates `folderId`) and shows a brief toast notification "Moved to [folder name]".
- [x] **Toast notifications** (`src/components/Toast.jsx`): A toast system for brief feedback messages. Toasts appear in the bottom-right corner, auto-dismiss after 4 seconds. Styles: success (green left border), error (red left border), info (blue left border). Used for: save confirmations, delete confirmations, send confirmations, error messages.
- [ ] **Inline title editing**: On the contracts list, double-clicking a contract title cell switches it to an inline input field. Press Enter to save, Escape to cancel. The input auto-focuses and selects all text.
- [x] **Keyboard shortcut: Cmd+K for search**: Global keyboard listener that opens the search modal when Cmd+K (Mac) or Ctrl+K (Windows) is pressed. Show the shortcut hint in the search button tooltip.
- [x] **Responsive sidebar collapse**: Add a hamburger/collapse button at the top of the sidebar. Clicking it collapses the sidebar to 56px width showing only icons (no text labels). The main content area expands. The collapse state persists in localStorage.

---

## Data Seed Notes

Implement in `createInitialData()` per `assets/data_model.md`. Key requirements for realistic agent training:

- [x] **Contract content**: Each contract must have realistic multi-paragraph HTML content that looks like a real legal document. Include section headings (h2), numbered clauses, party references, dates, and legal language. At least 500 words per contract. Templates should have similar content with `[PLACEHOLDER]` markers for variable data.
- [x] **Temporal realism**: Dates should be relative to "now" -- drafts created recently (1-7 days ago), pending contracts sent 1-5 days ago, signed contracts signed 2 weeks to 6 months ago, activity timestamps should be chronologically consistent within each contract's lifecycle.
- [x] **Relationship consistency**: Every signee's `contactId` or `userId` must reference a valid contact or user. Every contract's `folderId` must reference a valid folder. Every task's `contractId` must reference a valid contract. Activity entries must reference valid users/contacts.
- [x] **Status distribution**: Ensure at least 2-3 contracts in each status to test filtering. Pending contracts should have a mix of partially-signed (1 of 2 signees done) and fully-pending states.
- [x] **Notification variety**: Include at least 3 unread notifications to test the badge count. Cover multiple notification types.

---

## Out of Scope

Dev must NOT implement these:
- Authentication / login (app starts pre-logged-in as Sarah Chen, user-1)
- Real email sending (send actions update state only)
- Real file uploads or PDF generation
- Real e-signatures or identity verification
- Real API calls or network communication
- Database persistence beyond localStorage
- OAuth/SSO integration
- Payment processing or billing
- Real-time collaboration (multi-user editing)
